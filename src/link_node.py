# -*- coding: utf-8 -*-

generate_node()
random add_value()
# 两种: 1. 产生序列再一一赋值 2. 每次都调用赋值
for node(src) in nodes:
    graph.add_node(node)
    node.set_node_attributes
    for each node(dst) in graph:
        if possibility:
            graph.add_edge(src, dst)